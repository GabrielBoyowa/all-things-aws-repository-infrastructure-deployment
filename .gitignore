# Local terraform directory (contains binaries and modules)
.terraform/

# State files (should be in S3, not Git)
*.tfstate
*.tfstate.*

# Backend and Version constraints (The files you want to keep local only)
versions.tf
 backend.tf  

# Crash logs
crash.log

# Sensitive variable files
*.tfvars
*.tfvars.json

# Override files
override.tf
override.tf.json
